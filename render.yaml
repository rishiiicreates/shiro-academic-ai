    services:
      - type: web
        name: shiro-academic-backend
        runtime: docker
        dockerfilePath: ./Dockerfile
        plan: free
        region: oregon
        envVars:
          - key: GEMINI_API_KEY
            sync: false
          - key: PORT
            value: "8080"
    RENDER
    echo "✅ render.yaml created"
